
  # XBuild

  This is a code bundle for XBuild. The original project is available at https://www.figma.com/design/VUE58IYzXI3N6DQlsviXqv/XBuild.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  